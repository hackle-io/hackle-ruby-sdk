RSpec.describe Version::Ruby::Sdk do
  it "has a version number" do
    expect(Version::Ruby::Sdk::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
